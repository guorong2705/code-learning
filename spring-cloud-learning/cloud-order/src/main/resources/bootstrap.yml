spring:
  application:
    name: cloud-order-service
  cloud:
    consul:
      host: 127.0.0.1
      port: 8500
      config:
        enabled: true
        profileSeparator: '-'
      discovery:
        enabled: true
        heartbeat:
          enabled: true
